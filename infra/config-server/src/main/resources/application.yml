spring:
  application:
    name: config-server
  profiles:
    # files are in File System
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations:
            - classpath:config/infra/{application}/

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health, info

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/